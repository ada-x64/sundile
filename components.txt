Possible list of basic components and what they'd require:

RENDER PIPELINE:
. Model Class
. Shader Class
. Renderer Class
. c_Visible(Model* m, Shader* s)
	. setPos(float x, float y, float z);
	. 
. c_Camera()
	. setView(mat4 viewMatrix)
	. setLookat()