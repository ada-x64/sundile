# COMPONENTS - CMAKE

#file(GLOB_RECURSE COMPONENTS_FILES "*.h")
add_library(sundile_components INTERFACE)#${COMPONENTS_FILES})
#set_target_properties(SundileComponents PROPERTIES LINKER_LANGUAGE CXX)
